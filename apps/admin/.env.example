VITE_API_BASE_URL=http://localhost:3000/v1
VITE_DEFAULT_PROJECT_KEY=demo-project
VITE_DEFAULT_FLAG_KEY=new-checkout
# Presentation-only credentials. Values must match the backend DEMO_*_TOKEN
# variables and must never be reused as production credentials.
VITE_DEMO_ADMIN_TOKEN=replace-with-local-admin-demo-token
VITE_DEMO_DEVELOPER_TOKEN=replace-with-local-developer-demo-token
VITE_DEMO_VIEWER_TOKEN=replace-with-local-viewer-demo-token
