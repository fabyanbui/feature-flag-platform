---
agent: "agent"
description: "Create demo scenarios for the evaluation app."
---

Create two demo scenarios: global toggle and targeting/percentage rollout. For each, specify the user context, expected evaluation response (enabled + reason), and the gated UI behavior. Ensure the demo matches docs/requirement/demo/demo-app.md.
