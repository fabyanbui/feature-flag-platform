---
agent: "agent"
description: "Design a REST endpoint under /v1 with validation, errors, and audit logging."
---

Design the ${input:endpoint:METHOD /v1/path} API using JSON. Include request and response shapes, validation rules, error codes, and pagination (if list). Ensure it aligns with docs/requirement and docs/design. Call out how audit logging should capture mutations.
